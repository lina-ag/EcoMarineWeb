{{ form_start(form) }}
    <div class="form-grid">
        <div class="form-group-settings">
            {{ form_label(form.type, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.type, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(form.type) }}
        </div>

        <div class="form-group-settings">
            {{ form_label(form.quantite, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.quantite, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(form.quantite) }}
        </div>

        <div class="form-group-settings">
            {{ form_label(form.zone, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.zone, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(form.zone) }}
        </div>

        <div class="form-group-settings full-width">
            {{ form_label(form.description, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.description, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(form.description) }}
        </div>

        <div class="form-group-settings">
            {{ form_label(form.dateSignalement, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.dateSignalement, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(form.dateSignalement) }}
        </div>

        <div class="form-group-settings">
            {{ form_label(form.statut, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.statut, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(form.statut) }}
        </div>
    </div>

    <div class="btn-group">
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('app_dechet_index') }}" class="btn btn-secondary">Retour à la liste</a>
    </div>

    {{ form_rest(form) }}
{{ form_end(form) }}
