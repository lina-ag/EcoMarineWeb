{% extends 'base.html.twig' %}

{% block title %}Edit Dechet{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit Dechet</h2>
                <p class="card-subtitle">Modifier les informations du dechet.</p>
            </div>
        </div>

        {{ include('dechet/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_dechet_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('dechet/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
