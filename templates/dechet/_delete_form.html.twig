<form method="post" action="{{ path('app_dechet_delete', {'id_dechet': dechet.id_dechet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dechet.id_dechet) }}">
    <button class="btn btn-secondary" type="submit">Delete</button>
</form>
