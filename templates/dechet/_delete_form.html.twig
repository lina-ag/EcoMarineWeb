<form method="post" action="{{ path('app_dechet_delete', {'id_dechet': dechet.id_dechet}) }}" onsubmit="return confirm('Confirmer la suppression de ce signalement ?');" style="display:inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dechet.id_dechet) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>