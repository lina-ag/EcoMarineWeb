{% extends 'base.html.twig' %}

{% block title %}Edit ZonePlage{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit ZonePlage</h2>
                <p class="card-subtitle">Modifier les informations de la zone.</p>
            </div>
        </div>

        {{ include('zone_plage/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_zone_plage_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('zone_plage/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
