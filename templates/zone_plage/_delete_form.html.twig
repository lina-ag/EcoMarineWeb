<form method="post" action="{{ path('app_zone_plage_delete', {'id_zone': zone_plage.id_zone}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zone_plage.id_zone) }}">
    <button class="btn btn-secondary" type="submit">Delete</button>
</form>
