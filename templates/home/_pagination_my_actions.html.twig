{% if pageCount > 1 %}
    <nav class="pagination cleaning-pagination" aria-label="Pagination de mes actions de nettoyage">
        {% if previous is defined %}
            <a href="{{ path(route, query|merge({(pageParameterName): previous, section: 'nettoyage', subsection: 'my', _fragment: 'slide12'})) }}"
               class="cleaning-pagination__link"
               data-page-link
               aria-label="Page précédente">
                ‹
            </a>
        {% else %}
            <span class="disabled cleaning-pagination__link" aria-hidden="true">‹</span>
        {% endif %}

        {% for page in pagesInRange %}
            {% if page != current %}
                <a href="{{ path(route, query|merge({(pageParameterName): page, section: 'nettoyage', subsection: 'my', _fragment: 'slide12'})) }}"
                   class="cleaning-pagination__link"
                   data-page-link>
                    {{ page }}
                </a>
            {% else %}
                <span class="current cleaning-pagination__link" aria-current="page">{{ page }}</span>
            {% endif %}
        {% endfor %}

        {% if next is defined %}
            <a href="{{ path(route, query|merge({(pageParameterName): next, section: 'nettoyage', subsection: 'my', _fragment: 'slide12'})) }}"
               class="cleaning-pagination__link"
               data-page-link
               aria-label="Page suivante">
                ›
            </a>
        {% else %}
            <span class="disabled cleaning-pagination__link" aria-hidden="true">›</span>
        {% endif %}
    </nav>
{% endif %}
