<style>
    :root {
        --front-bg-soft: #eff8ff;
        --front-surface: rgba(255, 255, 255, 0.2);
        --front-surface-strong: rgba(255, 255, 255, 0.28);
        --front-border: rgba(255, 255, 255, 0.3);
        --front-border-soft: rgba(255, 255, 255, 0.38);
        --front-text: #ffffff;
        --front-text-muted: rgba(255, 255, 255, 0.82);
        --front-accent: #7dd3fc;
        --front-accent-dark: #38bdf8;
        --front-accent-soft: rgba(125, 211, 252, 0.18);
        --front-shadow: 0 28px 70px rgba(8, 47, 73, 0.2);
    }

    .front-video-bg {
        position: fixed;
        inset: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: -3;
        pointer-events: none;
        filter: saturate(1.02) brightness(0.78);
    }

    .front-video-overlay {
        position: fixed;
        inset: 0;
        z-index: -2;
        pointer-events: none;
        background:
            linear-gradient(180deg, rgba(34, 197, 246, 0.28) 0%, rgba(103, 232, 249, 0.3) 36%, rgba(186, 230, 253, 0.34) 100%),
            radial-gradient(circle at top left, rgba(125, 211, 252, 0.3), transparent 30%),
            radial-gradient(circle at bottom right, rgba(34, 211, 238, 0.18), transparent 26%);
    }

    .front-home-nav {
        position: sticky;
        top: 0;
        z-index: 5000;
        padding-top: 16px;
        pointer-events: auto;
    }

    .front-home-nav .navbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 1rem;
        padding: 1rem 1.4rem;
        margin: 0 auto;
        max-width: 1400px;
        border-radius: 30px;
        background: rgba(255, 255, 255, 0.14);
        border: 1px solid var(--front-border);
        box-shadow: 0 20px 50px rgba(8, 47, 73, 0.18);
        backdrop-filter: blur(20px);
        position: relative;
        z-index: 5001;
        pointer-events: auto;
    }

    .front-home-nav .navigation-row {
        display: flex;
        align-items: center;
        flex: 1;
        justify-content: flex-end;
    }

    .front-home-nav #navigation {
        display: flex;
        align-items: center;
        width: auto;
    }

    .front-home-nav .nav-box {
        display: flex;
        align-items: center;
        gap: 0.25rem;
    }

    .front-home-nav .navigation-menu {
        display: flex;
        align-items: center;
        margin: 0;
        padding: 0;
        list-style: none;
        gap: 0.25rem;
        flex-wrap: wrap;
    }

    .front-home-nav .navigation-menu li {
        margin: 0;
        pointer-events: auto;
    }

    .front-home-nav .navigation-menu li a,
    .front-home-nav .navigation-menu li .front-nav-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-height: 46px;
        padding: 0.7rem 1rem;
        color: #ffffff;
        background: transparent;
        border: none;
        cursor: pointer;
        appearance: none;
        text-decoration: none;
        transition: all 0.25s ease;
        font-weight: 600;
        border-radius: 999px;
        position: relative;
        z-index: 5002;
        pointer-events: auto;
        white-space: nowrap;
    }

    .front-home-nav .navigation-menu li a:hover,
    .front-home-nav .navigation-menu li .front-nav-btn:hover {
        background: rgba(255, 255, 255, 0.12);
        color: #ffffff;
    }

    .front-home-nav .navigation-menu li a.nav-action-link,
    .front-home-nav .navigation-menu li .front-nav-btn.nav-action-link {
        border: 1px solid rgba(125, 211, 252, 0.85);
        background: rgba(255, 255, 255, 0.08);
        color: #ffffff;
        font-weight: 700;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.15);
    }

    .front-home-nav .navigation-menu li a.is-active,
    .front-home-nav .navigation-menu li a.nav-action-link.is-active,
    .front-home-nav .navigation-menu li .front-nav-btn.is-active,
    .front-home-nav .navigation-menu li .front-nav-btn.nav-action-link.is-active {
        background: linear-gradient(135deg, rgba(125, 211, 252, 0.96) 0%, rgba(56, 189, 248, 0.96) 48%, rgba(14, 165, 233, 0.96) 100%);
        border-color: rgba(255, 255, 255, 0.15);
        color: #082f49;
        box-shadow: 0 12px 24px rgba(14, 165, 233, 0.34);
    }

    .front-home-nav .navigation-menu li a.nav-action-link:hover,
    .front-home-nav .navigation-menu li .front-nav-btn.nav-action-link:hover {
        background: rgba(255, 255, 255, 0.16);
        transform: translateY(-1px);
    }

    .front-home-nav .navbar-toggle {
        display: none;
        background: none;
        border: none;
        color: #ffffff;
        font-size: 1.5rem;
        cursor: pointer;
    }

    .front-page-shell {
        min-height: 100vh;
        position: relative;
        padding: 22px 20px 96px;
        background:
            radial-gradient(circle at top left, rgba(90, 200, 250, 0.14), transparent 32%),
            radial-gradient(circle at bottom right, rgba(0, 180, 216, 0.12), transparent 28%);
    }

    .front-form-shell {
        max-width: 1120px;
        margin: 36px auto 0;
        padding: 46px;
        border-radius: 34px;
        background:
            linear-gradient(180deg, rgba(255, 255, 255, 0.18) 0%, rgba(255, 255, 255, 0.12) 100%);
        border: 1px solid var(--front-border);
        box-shadow: var(--front-shadow);
        backdrop-filter: blur(22px);
        color: var(--front-text);
        overflow: hidden;
    }

    .front-form-shell .card-header {
        margin-bottom: 34px;
        padding-bottom: 22px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        text-align: center;
    }

    .front-form-shell .card-title {
        margin: 0 0 8px;
        color: var(--front-text);
        font-size: clamp(2rem, 4vw, 3rem);
        font-weight: 800;
        letter-spacing: -0.03em;
        text-transform: uppercase;
        line-height: 1.04;
    }

    .front-form-shell .card-subtitle {
        margin: 0;
        color: var(--front-text-muted);
        font-size: 1.06rem;
        max-width: 760px;
        line-height: 1.65;
        margin-left: auto;
        margin-right: auto;
    }

    .front-form-shell .reservation-admin-kicker {
        display: inline-block;
        margin-bottom: 10px;
        padding: 0.32rem 0.72rem;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.14);
        color: #ffffff;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .front-form-shell .reservation-form,
    .front-form-shell .activity-form {
        display: block;
    }

    .front-form-shell .form-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 26px;
    }

    .front-form-shell .full-width {
        grid-column: 1 / -1;
    }

    .front-form-shell .form-group-settings {
        display: flex;
        flex-direction: column;
        gap: 11px;
    }

    .front-form-shell .form-label,
    .front-form-shell label.form-label,
    .front-form-shell .checkbox-label,
    .front-form-shell label {
        color: #ffffff;
        font-weight: 700;
        font-size: 1rem;
        margin-bottom: 0;
        letter-spacing: -0.01em;
    }

    .front-form-shell .form-input,
    .front-form-shell input.form-input,
    .front-form-shell select.form-input,
    .front-form-shell textarea.form-input {
        width: 100%;
        min-height: 64px;
        padding: 0 22px;
        border-radius: 22px;
        border: 1px solid rgba(255, 255, 255, 0.34);
        background: rgba(255, 255, 255, 0.2);
        color: #ffffff;
        box-shadow: inset 0 1px 2px rgba(255, 255, 255, 0.04);
        transition: border-color 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease;
        font-size: 1rem;
        backdrop-filter: blur(12px);
    }

    .front-form-shell textarea.form-input {
        min-height: 190px;
        padding: 18px 22px;
        line-height: 1.6;
        resize: vertical;
    }

    .front-form-shell select.form-input {
        padding-right: 52px;
        background-image: linear-gradient(45deg, transparent 50%, #64748b 50%), linear-gradient(135deg, #64748b 50%, transparent 50%);
        background-position: calc(100% - 24px) calc(50% - 3px), calc(100% - 18px) calc(50% - 3px);
        background-size: 6px 6px, 6px 6px;
        background-repeat: no-repeat;
        appearance: none;
    }

    .front-form-shell .form-input::placeholder {
        color: rgba(255, 255, 255, 0.72);
    }

    .front-form-shell .form-input:focus {
        outline: none;
        border-color: rgba(255, 255, 255, 0.72);
        box-shadow: 0 0 0 4px rgba(125, 211, 252, 0.2);
        background: rgba(255, 255, 255, 0.26);
    }

    .front-form-shell .btn-group {
        display: flex;
        gap: 14px;
        margin-top: 32px;
        flex-wrap: wrap;
        align-items: center;
    }

    .front-form-shell .btn.btn-primary {
        width: 100%;
        min-height: 62px;
        border: none;
        border-radius: 999px;
        background: linear-gradient(135deg, #7dd3fc 0%, #38bdf8 45%, #0ea5e9 100%);
        color: #083344;
        font-size: 1rem;
        font-weight: 800;
        letter-spacing: 0.04em;
        text-transform: uppercase;
        box-shadow: 0 18px 35px rgba(14, 165, 233, 0.24);
        transition: transform 0.22s ease, box-shadow 0.22s ease, filter 0.22s ease;
    }

    .front-form-shell .btn.btn-primary:hover {
        transform: translateY(-2px);
        filter: saturate(1.05);
        box-shadow: 0 24px 40px rgba(14, 165, 233, 0.28);
    }

    .front-form-shell .btn.btn-secondary {
        min-height: 62px;
        border-radius: 999px;
        border: 1px solid rgba(255, 255, 255, 0.34);
        background: rgba(255, 255, 255, 0.16);
        color: #ffffff;
        font-weight: 700;
        padding: 0 22px;
    }

    .front-form-shell .invalid-feedback,
    .front-form-shell .form-error-message,
    .front-form-shell .form-errors,
    .front-form-shell .activity-recurrence-preview,
    .front-form-shell small,
    .front-form-shell ul {
        color: rgba(255, 255, 255, 0.86);
    }

    .front-form-shell .invalid-feedback,
    .front-form-shell .form-errors {
        color: #ffe4e6;
        font-weight: 600;
    }

    .front-form-shell .activity-recurrence-shell {
        padding: 22px;
        border-radius: 24px;
        background: linear-gradient(180deg, rgba(255, 255, 255, 0.14) 0%, rgba(255, 255, 255, 0.1) 100%);
        border: 1px solid rgba(255, 255, 255, 0.24);
    }

    .front-form-shell .activity-recurrence-days {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
    }

    .front-form-shell .activity-recurrence-chip {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 11px 15px;
        border-radius: 999px;
        background: rgba(255, 255, 255, 0.16);
        border: 1px solid rgba(255, 255, 255, 0.28);
    }

    .front-form-shell .activity-recurring-toggle {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 10px 0 0;
    }

    .front-form-shell .capacity-select-shell,
    .front-form-shell .reservation-rating {
        width: 100%;
    }

    .front-form-shell .capacity-select-shell .form-input {
        width: 100%;
    }

    @media (max-width: 992px) {
        .front-home-nav .navbar-toggle {
            display: block;
        }

        .front-home-nav .nav-box {
            display: none;
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            background: rgba(255, 255, 255, 0.18);
            flex-direction: column;
            padding: 1rem;
            z-index: 1000;
            border-radius: 18px;
            margin-top: 10px;
            border: 1px solid rgba(255, 255, 255, 0.24);
            box-shadow: 0 18px 40px rgba(8, 47, 73, 0.22);
            backdrop-filter: blur(18px);
        }

        .front-home-nav .nav-box.active {
            display: flex;
        }

        .front-home-nav .navigation-menu {
            flex-direction: column;
            width: 100%;
            text-align: center;
        }

        .front-home-nav .navigation-menu li,
        .front-home-nav .navigation-menu li a,
        .front-home-nav .navigation-menu li .front-nav-btn {
            width: 100%;
        }
    }

    @media (max-width: 768px) {
        .front-page-shell {
            padding: 16px 16px 50px;
        }

        .front-form-shell {
            margin-top: 24px;
            padding: 26px 22px;
            border-radius: 22px;
        }

        .front-form-shell .form-grid {
            grid-template-columns: 1fr;
        }

        .front-form-shell .full-width {
            grid-column: auto;
        }

        .front-form-shell .card-header {
            margin-bottom: 24px;
            padding-bottom: 18px;
        }

        .front-form-shell .card-title {
            font-size: 2rem;
        }
    }
</style>
