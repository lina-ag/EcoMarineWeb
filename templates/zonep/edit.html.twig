{% extends 'base.html.twig' %}

{% block title %}Edit Zonep{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit Zone</h2>
                <p class="card-subtitle">Modifier le nom, la categorie et le statut.</p>
            </div>
        </div>

        {{ include('zonep/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_zonep_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('zonep/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
