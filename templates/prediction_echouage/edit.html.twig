{% extends 'base.html.twig' %}

{% block title %}Edit PredictionEchouage{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit PredictionEchouage</h2>
                <p class="card-subtitle">Modifier les valeurs de risque et recommandations.</p>
            </div>
        </div>

        {{ include('prediction_echouage/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_prediction_echouage_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('prediction_echouage/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
