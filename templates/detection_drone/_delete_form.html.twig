<form method="post" action="{{ path('app_detection_drone_delete', {'id_detection': detection_drone.id_detection}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detection_drone.id_detection) }}">
    <button class="btn btn-secondary" type="submit">Delete</button>
</form>
