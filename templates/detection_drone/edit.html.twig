{% extends 'base.html.twig' %}

{% block title %}Edit DetectionDrone{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit DetectionDrone</h2>
                <p class="card-subtitle">Modifier les informations de detection.</p>
            </div>
        </div>

        {{ include('detection_drone/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_detection_drone_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('detection_drone/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
