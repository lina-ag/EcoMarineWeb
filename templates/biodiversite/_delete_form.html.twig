<form method="post" action="{{ path('app_biodiversite_delete', {'id': biodiversite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ biodiversite.id) }}">
    <button class="btn">Delete</button>
</form>
