{% extends 'base.html.twig' %}

{% block title %}Edit Biodiversite{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit Biodiversite</h2>
                <p class="card-subtitle">Modifier espece, zone, nombre et date.</p>
            </div>
        </div>

        {{ include('biodiversite/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_biodiversite_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('biodiversite/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
