<form method="post" action="{{ path('app_survzone_delete', {'idSurv': survzone.idSurv}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ survzone.idSurv) }}">
    <button class="btn">Delete</button>
</form>
