{% extends 'base.html.twig' %}

{% block title %}{{ 'survzone.edit_title'|trans }}{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">{{ 'survzone.edit_title'|trans }}</h2>
                <p class="card-subtitle">{{ 'survzone.edit_subtitle'|trans }}</p>
            </div>
        </div>

        {{ include('survzone/_form.html.twig', {'button_label': 'common.update'|trans}) }}

        <div class="btn-group">
            <a href="{{ path('app_survzone_index') }}" class="btn btn-secondary">{{ 'common.back_to_list'|trans }}</a>
            {{ include('survzone/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}