{% extends 'base.html.twig' %}

{% block title %}Edit Survzone{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit Survzone</h2>
                <p class="card-subtitle">Mettre a jour la date, l'observation et l'identifiant de zone.</p>
            </div>
        </div>

        {{ include('survzone/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_survzone_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('survzone/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
