{% extends 'base.html.twig' %}

{% block title %}Edit MissionDrone{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit MissionDrone</h2>
                <p class="card-subtitle">Modifier les details de la mission.</p>
            </div>
        </div>

        {{ include('mission_drone/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_mission_drone_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('mission_drone/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
