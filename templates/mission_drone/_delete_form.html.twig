<form method="post" action="{{ path('app_mission_drone_delete', {'id_mission': mission_drone.id_mission}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission_drone.id_mission) }}">
    <button class="btn">Delete</button>
</form>
