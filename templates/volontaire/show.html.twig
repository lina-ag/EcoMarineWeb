{% extends 'base.html.twig' %}

{% block title %}Volontaire{% endblock %}

{% block body %}
    <h1>Volontaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_volontaire</th>
                <td>{{ volontaire.idVolontaire }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ volontaire.nom }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ volontaire.contact }}</td>
            </tr>
            <tr>
                <th>Id_action</th>
                <td>{{ volontaire.idAction }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_volontaire_index') }}">back to list</a>

    <a href="{{ path('app_volontaire_edit', {'id_volontaire': volontaire.id_volontaire}) }}">edit</a>

    {{ include('volontaire/_delete_form.html.twig') }}
{% endblock %}
