{% extends 'base_admin.html.twig' %}
{% block title %}Edit Volontaire{% endblock %}

{% block admin_content %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit Volontaire</h2>
                <p class="card-subtitle">Modifier les informations du volontaire.</p>
            </div>
        </div>

        {{ include('volontaire/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            {{ include('volontaire/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}