<form method="post" action="{{ path('app_volontaire_delete', {'id_volontaire': volontaire.id_volontaire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ volontaire.id_volontaire) }}">
    <button class="btn">Delete</button>
</form>
