<form method="post" action="{{ path('app_activite_ecologique_delete', {'id_activite': activite_ecologique.id_activite}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activite_ecologique.id_activite) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>
