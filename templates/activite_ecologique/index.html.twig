{% extends 'base_admin.html.twig' %}

{% block page_title %}ActivitÃƒÂ©s ÃƒÂ©cologiques{% endblock %}
{% block navbar_title %}ActivitÃƒÂ©s ÃƒÂ©cologiques{% endblock %}

{% block admin_content %}
    <section class="glass-card table-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Liste des activitÃƒÂ©s ÃƒÂ©cologiques</h2>
                <p class="card-subtitle">Suivi des activitÃƒÂ©s enregistrÃƒÂ©es dans la plateforme.</p>
            </div>
            <div class="card-actions">
                <a href="{{ path('app_activite_ecologique_new') }}" class="btn btn-primary">CrÃƒÂ©er une activitÃƒÂ©</a>
            </div>
        </div>

        <div class="table-wrapper">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Id activitÃƒÂ©</th>
                        <th>Nom activitÃƒÂ©</th>
                        <th>Date activitÃƒÂ©</th>
                        <th>CapacitÃƒÂ©</th>
                        <th>Description</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for activite_ecologique in activite_ecologiques %}
                    <tr>
                        <td>{{ activite_ecologique.idActivite }}</td>
                        <td>{{ activite_ecologique.nomActivite }}</td>
                        <td>{{ activite_ecologique.dateActivite ? activite_ecologique.dateActivite|date('Y-m-d') : '' }}</td>
                        <td>{{ activite_ecologique.capacite }}</td>
                        <td>{{ activite_ecologique.description }}</td>
                        <td>
                            <a href="{{ path('app_activite_ecologique_show', {'id_activite': activite_ecologique.id_activite}) }}" class="btn btn-secondary">Voir</a>
                            <a href="{{ path('app_activite_ecologique_edit', {'id_activite': activite_ecologique.id_activite}) }}" class="btn btn-primary">Modifier</a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6">Aucun enregistrement trouvÃƒÂ©</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </section>
{% endblock %}
