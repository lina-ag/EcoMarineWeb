{% extends 'base.html.twig' %}

{% block title %}Edit Observation{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit Observation</h2>
                <p class="card-subtitle">Modifier les donnees de l observation.</p>
            </div>
        </div>

        {{ include('observation/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_observation_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('observation/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
