{% extends 'base.html.twig' %}

{% block title %}Edit ActionNettoyage{% endblock %}

{% block body %}
    <section class="glass-card">
        <div class="card-header">
            <div>
                <h2 class="card-title">Edit ActionNettoyage</h2>
                <p class="card-subtitle">Modifier la date et le lieu de l action.</p>
            </div>
        </div>

        {{ include('action_nettoyage/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="btn-group">
            <a href="{{ path('app_action_nettoyage_index') }}" class="btn btn-secondary">back to list</a>
            {{ include('action_nettoyage/_delete_form.html.twig') }}
        </div>
    </section>
{% endblock %}
