{% extends 'base.html.twig' %}

{% block title %}FauneMarine{% endblock %}

{% block body %}
    <h1>FauneMarine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id_animal</th>
                <td>{{ faune_marine.idAnimal }}</td>
            </tr>
            <tr>
                <th>Espece</th>
                <td>{{ faune_marine.espece }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ faune_marine.etat }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ faune_marine.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_faune_marine_index') }}">back to list</a>

    <a href="{{ path('app_faune_marine_edit', {'id_animal': faune_marine.id_animal}) }}">edit</a>

    {{ include('faune_marine/_delete_form.html.twig') }}
{% endblock %}
