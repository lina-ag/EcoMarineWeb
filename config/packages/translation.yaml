framework:

    default_locale: en
    translator:
        default_path: '%kernel.project_dir%/translations'


    default_locale: fr

    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - fr
            - en

        providers:
